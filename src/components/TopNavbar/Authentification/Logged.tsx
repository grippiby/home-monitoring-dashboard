import style from './Logged.module.css'
import logOutIco from '../../../assets/ico/btn - Sign Out.svg'
import myPhoto from '../../../assets/tmp/avatar.png'
/* const setActive = (navData) =>
	navData.isActive ? style.active : style.sidebar_text */
const Logged = (props) => {
	return <div className={style.container}></div>
}

export default Logged
